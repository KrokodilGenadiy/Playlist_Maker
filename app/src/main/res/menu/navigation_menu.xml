<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/searchFragment"
        android:icon="@drawable/search_menu"
        android:title="@string/menu_search_title"
        app:showAsAction="withText"/>
    <item
        android:id="@+id/mediaFragment"
        android:icon="@drawable/media_menu"
        android:title="@string/menu_media_title"
        app:showAsAction="withText"/>
    <item
        android:id="@+id/settingsFragment"
        android:icon="@drawable/settings_menu"
        android:title="@string/menu_settings_title"
        app:showAsAction="withText"/>
</menu>